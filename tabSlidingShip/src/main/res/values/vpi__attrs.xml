<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2012 Jake Wharton
     Copyright (C) 2011 Patrik Åkerfeldt

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
     See the License for the specific language governing permissions and
     limitations under the License.
-->

<resources>
    <declare-styleable name="ViewPagerIndicator1">
        <!-- Style of the circle indicator. -->
        <attr name="vpiCirclePageIndicatorStyle1" format="reference"/>
        <!-- Style of the icon indicator's views. -->
        <attr name="vpiIconPageIndicatorStyle1" format="reference"/>
        <!-- Style of the line indicator. -->
        <attr name="vpiLinePageIndicatorStyle1" format="reference"/>
        <!-- Style of the title indicator. -->
        <attr name="vpiTitlePageIndicatorStyle1" format="reference"/>
        <!-- Style of the tab indicator's tabs. -->
        <attr name="vpiTabPageIndicatorStyle1" format="reference"/>
        <!-- Style of the underline indicator. -->
        <attr name="vpiUnderlinePageIndicatorStyle1" format="reference"/>
    </declare-styleable>

    <attr name="centered1" format="boolean" />
    <attr name="selectedColor1" format="color" />
    <attr name="strokeWidth1" format="dimension" />
    <attr name="unselectedColor1" format="color" />

    <declare-styleable name="CirclePageIndicator1">
        <!-- Whether or not the indicators should be centered. -->
        <attr name="centered1" />
        <!-- Color of the filled circle that represents the current page. -->
        <attr name="fillColor1" format="color" />
        <!-- Color of the filled circles that represents pages. -->
        <attr name="pageColor1" format="color" />
        <!-- Orientation of the indicator. -->
        <attr name="android:orientation"/>
        <!-- Radius of the circles. This is also the spacing between circles. -->
        <attr name="radius1" format="dimension" />
        <!-- Whether or not the selected indicator snaps to the circles. -->
        <attr name="snap1" format="boolean" />
        <!-- Color of the open circles. -->
        <attr name="strokeColor1" format="color" />
        <!-- Width of the stroke used to draw the circles. -->
        <attr name="strokeWidth1" />
        <!-- View background -->
        <attr name="android:background"/>
    </declare-styleable>

    <declare-styleable name="LinePageIndicator1">
        <!-- Whether or not the indicators should be centered. -->
        <attr name="centered1" />
        <!-- Color of the unselected lines that represent the pages. -->
        <attr name="unselectedColor1" />
        <!-- Color of the selected line that represents the current page. -->
        <attr name="selectedColor1" />
        <!-- Width of each indicator line. -->
        <attr name="lineWidth1" format="dimension" />
        <!-- Width of each indicator line's stroke. -->
        <attr name="strokeWidth1" />
        <!-- Width of the gap between each indicator line. -->
        <attr name="gapWidth1" format="dimension" />
        <!-- View background -->
        <attr name="android:background"/>
    </declare-styleable>

    <declare-styleable name="TitlePageIndicator1">
        <!-- Screen edge padding. -->
        <attr name="clipPadding1" format="dimension" />
        <!-- Color of the footer line and indicator. -->
        <attr name="footerColor1" format="color" />
        <!-- Height of the footer line. -->
        <attr name="footerLineHeight1" format="dimension" />
        <!-- Style of the indicator. Default is triangle. -->
        <attr name="footerIndicatorStyle1">
            <enum name="none" value="0" />
            <enum name="triangle" value="1" />
            <enum name="underline" value="2" />
        </attr>
        <!-- Height of the indicator above the footer line. -->
        <attr name="footerIndicatorHeight1" format="dimension" />
        <!-- Left and right padding of the underline indicator. -->
        <attr name="footerIndicatorUnderlinePadding1" format="dimension" />
        <!-- Padding between the bottom of the title and the footer. -->
        <attr name="footerPadding1" format="dimension" />
        <!-- Position of the line. -->
        <attr name="linePosition1">
            <enum name="bottom" value="0"/>
            <enum name="top" value="1"/>
        </attr>
        <!-- Color of the selected title. -->
        <attr name="selectedColor1" />
        <!-- Whether or not the selected item is displayed as bold. -->
        <attr name="selectedBold1" format="boolean" />
        <!-- Color of regular titles. -->
        <attr name="android:textColor" />
        <!-- Size of title text. -->
        <attr name="android:textSize" />
        <!-- Padding between titles when bumping into each other. -->
        <attr name="titlePadding1" format="dimension" />
        <!-- Padding between titles and the top of the View. -->
        <attr name="topPadding1" format="dimension" />
        <!-- View background -->
        <attr name="android:background"/>
    </declare-styleable>

    <declare-styleable name="UnderlinePageIndicator1">
        <!-- Whether or not the selected indicator fades. -->
        <attr name="fades1" format="boolean" />
        <!-- Length of the delay to fade the indicator. -->
        <attr name="fadeDelay1" format="integer" />
        <!-- Length of the indicator fade to transparent. -->
        <attr name="fadeLength1" format="integer" />
        <!-- Color of the selected line that represents the current page. -->
        <attr name="selectedColor1" />
        <!-- View background -->
        <attr name="android:background"/>
    </declare-styleable>
</resources>
